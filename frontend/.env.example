# API Configuration
# Relative URL works in Docker (nginx proxies /api/ to backend)
# Set full URL for standalone frontend (e.g. http://localhost:3000/api/v1)
VITE_API_BASE_URL=/api/v1

# Environment
VITE_NODE_ENV=development
